let number1 = prompt("Enter the Number");
let decimal = prompt("Enter the decimals");
function uptoDecimals(num, dec) {
    if ((typeof num !== 'number') || (typeof d !== 'number'))
      return false;
           num = parseFloat(num) || 0;
       return num.toFixed(dec);
       }

alert(uptoDecimals(number1, decimal))
